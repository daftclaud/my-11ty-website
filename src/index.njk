---
title: Home
layout: base.njk
---

<div class="content">
  <img
    src="https://media1.tenor.com/m/qRC04kNvXAIAAAAC/smiley-clouds.gif"
    alt="Smiley"
    loading="lazy"
    decoding="async"
  />
  <p>
    Hello! Welcome to my awesome personal homepage. This site is all about me
    and my interests.
  </p>
  <br />
  <p>
    Check out my <a href="/word-curations">Word Curations</a> page where I share
    interesting words and their meanings!
  </p>
  <br />
  <p>
    Take a look at my <a href="/pens">Pens</a> page
    to explore my collection.
  </p>
</div>

<section class="recent-activity">
  <h2>Recent Activity</h2>
  
  <div class="activity-section word-curation-section">
    <h3>Latest Word Curation</h3>
    {% if collections.recentWordCurations.length > 0 %}
      {% set curation = collections.recentWordCurations[0] %}
      <div class="latest-curation">
        <a href="{{ curation.url }}" class="curation-date">{{ curation.data.date | formatDate }}</a>
        <ul class="words-list">
          {% for word in curation.data.words %}
            <li>
              <strong>{{ word.word }}</strong> - {{ word.definition }}
              {% if word.source %}<span class="word-source">Source: {{ word.source }}</span>{% endif %}
            </li>
          {% endfor %}
        </ul>
      </div>
    {% else %}
      <p>No recent curations found.</p>
    {% endif %}
  </div>

  <div class="activity-section pens-section">
    <h3>Latest Pens Added</h3>
    {% if collections.recentPens.length > 0 %}
      <div class="pens-grid">
        {% for pen in collections.recentPens %}
          <a href="{{ pen.url }}" class="pen-card">
            <img src="{{ pen.data.image }}" alt="{{ pen.data.title }}" class="pen-thumbnail" loading="lazy" decoding="async">
            <div class="pen-info">
              <div class="pen-brand">{{ pen.data.brand }}</div>
              <div class="pen-model">{{ pen.data.title | replace(pen.data.brand, "") | replace(":", "") | trim }}</div>
            </div>
          </a>
        {% endfor %}
      </div>
    {% else %}
      <p>No pens found.</p>
    {% endif %}
  </div>
</section>

<style>
  .content img {
    max-width: 100%;
    margin-bottom: 20px;
  }
  
  @media (min-width: 600px) {
    .content img {
      max-width: 50%;
      float: left;
      margin-right: 20px;
      margin-bottom: 0;
    }
    .content {
      display: flow-root;
    }
  }

  .recent-activity {
    margin-top: 40px;
    padding: 15px;
    background-color: #f0f0f0;
    border: 3px dashed #ff00ff;
    border-radius: 10px;
  }
  
  @media (min-width: 600px) {
    .recent-activity {
      padding: 20px;
    }
  }
  
  @media (min-width: 768px) {
    .recent-activity {
      padding: 30px;
    }
  }
  
  [data-theme="dark"] .recent-activity {
    background-color: #0f3460;
    border-color: #ff6b35;
  }
  
  [data-theme="dark"] .latest-curation,
  [data-theme="dark"] .pen-card {
    background-color: #1a1a2e;
  }

  .recent-activity h2 {
    color: #ff4500;
    text-align: center;
    margin-top: 0;
    margin-bottom: 20px;
    text-shadow: 1px 1px #000000;
  }
  
  [data-theme="dark"] .recent-activity h2 {
    color: #ff6b35;
    text-shadow: 1px 1px #0f3460;
  }

  .activity-section {
    margin-bottom: 30px;
  }

  .activity-section:last-child {
    margin-bottom: 0;
  }

  .activity-section h3 {
    color: #00a0a0;
    margin-bottom: 15px;
    font-size: 1.2em;
  }
  
  [data-theme="dark"] .activity-section h3 {
    color: #00d4ff;
  }

  /* Word Curation Styles */
  .latest-curation {
    background-color: #ffffff;
    padding: 15px;
    border-radius: 8px;
    border-left: 4px solid #00ffff;
  }
  
  @media (min-width: 600px) {
    .latest-curation {
      padding: 20px;
    }
  }
  
  [data-theme="dark"] .latest-curation {
    border-left-color: #00d4ff;
  }

  .curation-date {
    display: block;
    font-size: 1.1em;
    font-weight: bold;
    color: #0066cc;
    text-decoration: none;
    margin-bottom: 15px;
  }

  .curation-date:hover {
    text-decoration: underline;
  }
  
  .curation-date:focus {
    outline: 2px solid #0066cc;
    outline-offset: 2px;
    border-radius: 2px;
  }
  
  [data-theme="dark"] .curation-date {
    color: #00d4ff;
  }

  .words-list {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .words-list li {
    padding: 10px 0;
    border-bottom: 1px solid #e0e0e0;
  }
  
  [data-theme="dark"] .words-list li {
    border-bottom-color: #333;
  }

  .words-list li:last-child {
    border-bottom: none;
  }

  .words-list strong {
    color: #ff00ff;
    font-size: 1.05em;
  }
  
  [data-theme="dark"] .words-list strong {
    color: #ff6b35;
  }

  .word-source {
    display: block;
    font-size: 0.85em;
    color: #888;
    margin-top: 5px;
    font-style: italic;
  }
  
  [data-theme="dark"] .word-source {
    color: #999;
  }

  /* Pens Grid Styles */
  .pens-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(140px, 1fr));
    gap: 12px;
  }
  
  @media (min-width: 480px) {
    .pens-grid {
      grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
      gap: 15px;
    }
  }
  
  @media (min-width: 768px) {
    .pens-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .pen-card {
    background-color: #ffffff;
    border: 2px solid #00ffff;
    border-radius: 8px;
    overflow: hidden;
    text-decoration: none;
    transition: transform 0.2s, box-shadow 0.2s;
    display: flex;
    flex-direction: column;
    min-height: 200px;
  }
  
  @media (hover: hover) {
    .pen-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 5px 15px rgba(255, 0, 255, 0.3);
      border-color: #ff00ff;
    }
  }
  
  .pen-card:focus {
    outline: 3px solid #ff00ff;
    outline-offset: 2px;
  }
  
  [data-theme="dark"] .pen-card {
    border-color: #00d4ff;
  }
  
  [data-theme="dark"] .pen-card:hover {
    border-color: #ff6b35;
    box-shadow: 0 5px 15px rgba(255, 107, 53, 0.3);
  }

  .pen-thumbnail {
    width: 100%;
    height: 150px;
    object-fit: cover;
    background-color: #f5f5f5;
  }
  
  [data-theme="dark"] .pen-thumbnail {
    background-color: #2a2a3e;
  }

  .pen-info {
    padding: 10px;
    text-align: center;
  }

  .pen-brand {
    font-weight: bold;
    color: #0066cc;
    font-size: 0.95em;
    margin-bottom: 3px;
  }
  
  [data-theme="dark"] .pen-brand {
    color: #00d4ff;
  }

  .pen-model {
    font-size: 0.8em;
    color: #666;
    line-height: 1.2;
  }
  
  [data-theme="dark"] .pen-model {
    color: #aaa;
  }
</style>
